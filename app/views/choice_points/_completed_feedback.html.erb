<div class="feedback-report-container">
  <% if choice_point.successful %>
    <h3 class="feedback-heading">
      <span class="feedback-chosen-option"><%= choice_point.chosen.description %></span> was successful! 🥳
    </h3>
    <p>Everyone who voted for <strong><%= choice_point.chosen.description %></strong> gets +5 reputation.</p>

  <% else %>
    <h3 class="feedback-heading">
      <span class="feedback-chosen-option"><%= choice_point.chosen.description %></span> was unsuccessful. 😢
    </h3>
    <p>ChoicePoint isn’t perfect, but we do our best!</p>

  <% end %>
</div>
